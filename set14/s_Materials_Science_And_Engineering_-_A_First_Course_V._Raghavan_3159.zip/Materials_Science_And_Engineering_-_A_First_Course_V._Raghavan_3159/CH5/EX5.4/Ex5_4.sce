errcatch(-1,"stop");mode(2);// find the size of largest sphere that can fit into a tetrahedral void

r = 1 // let
a = 3/4 
printf("\n Example 5.4")
pt = 2*sqrt(2/3)*r
s = a*pt-r // size of sphere
printf("\n Size of largest sphere that can fit into a tetrahedral void is %.3fr",s)


exit();

errcatch(-1,"stop");mode(2);// Exa 2.17
;
;

// Given data
V_dc = 100;// in V
V_m = (V_dc*%pi)/2;// in V
PIV = 2*V_m;// in V
disp(PIV,"Peak inverse voltage for center tapped FWR in V is");
PIV1 = V_m;// in V
disp(PIV1,"Peak inverse voltage for bridge type FWR in V is");

exit();
